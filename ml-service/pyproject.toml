[project]
name = "marketpulse-ml-service"
version = "0.1.0"
description = "MarketPulse NLP and machine-learning service foundation"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
